# This is the main FastAPI application.
# It should define the API endpoints that receive data from IoT sensors.
# Each endpoint must parse the payload, validate it, and insert it into the database.
# You will also handle exceptions and return appropriate HTTP responses.
