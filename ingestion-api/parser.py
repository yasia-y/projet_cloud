# This module should implement the logic to decode incoming data from base64 and msgpack formats.
# The result should be a Python dictionary representing the sensor data with readable keys and values.
